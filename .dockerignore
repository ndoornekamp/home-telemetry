# Docker build sends a whole directory to the docker host.
# The contents of .git and venv are big and useless, so ignore then.
.git

# IDEs
.vscode

# Caches
**/.*_cache
**/.coverage
**/coverage.xml
**/__pycache__

# Poetry lockfile (Dockerfile uses requirements.txt - generated by poetry - so the Docker image doesn't need poetry)
poetry.lock

# Build directories
**/build
**/dist

# Tests
**/tests
**/test_resources

# Infra
infra/*
